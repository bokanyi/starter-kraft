import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vkv-archistart',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
